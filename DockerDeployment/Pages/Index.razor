@page "/"

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Please upload  deplyoment package (.zip format)


<div class="input-group mb-3">
  <input type="text" class="form-control" placeholder="file path of docker images" @bind="WorkingDirectory">
  <div class="input-group-append">
    <button class="btn btn-outline-secondary" type="submit" @onclick="OnClickButton">Deploy</button>
  </div>
</div>


@*@if (_fileName != null)
{
    <h3>@_fileName</h3>
    if (_entries.Any())
    {
        <ul>
            @foreach (var item in _entries.Where(x => !x.Name.Equals("Dockerfile")))
            {
                <li>
                    <h4>@item.Name</h4>
                    <pre>@item.Content</pre>
                </li>
            }
        </ul>
    }
}*@